import { Fragment } from "react";
import Enter from './Enter';
/* IDE 화면 임시 대체해서 만든 페이지, 이후 Enter 컴포넌트만 가져다 쓸 예정 */
function ChatPage() {   
    return (
        <Fragment>
            <div><Enter /></div>
        </Fragment>
    )
}
export default ChatPage;